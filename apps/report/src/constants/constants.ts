import { EasyLabsSex } from '@easy-labs-int/shared';

// const date = new Date();
export const initialProfile = {
  sex: EasyLabsSex.FEMALE,
  age: 50,
  // smoke: false,
  // pregnant: false,
  // bloodPressure: '',
  // weight: 0,
  // height: '_\' __"',
  // testDate: `${date.getFullYear()}-${date.getMonth() > 10 ? `${date.getMonth()}` : `0${date.getMonth()}`}-${
  //   date.getDay() > 10 ? `${date.getDay()}` : `0${date.getDay()}`
  // }`,
};
