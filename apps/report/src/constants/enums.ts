export enum TypeContent {
  RESULT = 'result',
  REPORT = 'report',
}

export enum InputsTypes {
  SWITCH = 'switch',
  NUMBER = 'number',
  DEFAULT = '',
}

export enum MimeType {
  JSON = 'application/json',
}
